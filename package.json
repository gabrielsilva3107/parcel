{
  "name": "parcel",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rimraf .parcel-cache",
    "dev": "npm run clean && parcel src/index.html src/styles/main.scss src/scripts/main.js",
    "build": "parcel build src/index.html --no-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.0",
    "parcel": "^2.8.0",
    "rimraf": "^6.0.1",
    "sharp": "^0.31.3"
  }
}
